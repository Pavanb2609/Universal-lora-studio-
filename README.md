# Universal-lora-studio-
Plan LoRA/QLoRA training on any hardware. Detects NVIDIA, AMD, Apple Silicon or CPU-only machines, estimates memory component by component, and generates a runnable config that scales from an 8GB laptop to a multi-GPU server. Explains every choice, never overrides yours, and labels each number Detected, Configured or Estimated.
